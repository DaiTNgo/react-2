export enum StatusMachine {
    IDLE = "idle",
    STOP = "stop",
    START = "start",
    PENDING = "pending",
    INCORRECT = "incorrect",
    CORRECT = "correct",
}

export enum StatusAudio {
    PLAY = "PLAY",
    PAUSE = "PAUSE",
    IDLE = "IDLE",
    STOP = "STOP",
    RESUME = "RESUME",
}
